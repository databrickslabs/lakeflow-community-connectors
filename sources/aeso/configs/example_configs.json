{
  "_description": "Example AESO connection configurations (Unity Catalog connection)",
  
  "connection_config": {
    "api_key": "YOUR_API_KEY"
  },
  
  "_note": "start_date and lookback_hours are now configured per-table in table_configuration, not at connection level",
  
  "_see_also": "pipeline_spec_example.json for complete table configuration examples"
}


